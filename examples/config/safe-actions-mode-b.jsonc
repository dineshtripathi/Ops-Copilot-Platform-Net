// ────────────────────────────────────────────────────────────
// SafeActions — Mode B (Azure Read-Only)
//
// In Mode B the global kill-switch is ON, but only probe and
// Azure-read action types are enabled.  No write or restart
// actions can execute.
//
// Copy this block into appsettings.Development.json (or
// appsettings.json) under the root object.
// ────────────────────────────────────────────────────────────
{
  "SafeActions": {
    // ── Global kill-switch ──────────────────────────────────
    "EnableExecution": true,

    // ── Feature flags for read-only ─────────────────────────
    "EnableRealHttpProbe":              true,
    "EnableAzureReadExecutions":        true,
    "EnableAzureMonitorReadExecutions": true,

    // ── Tenant allow-list ───────────────────────────────────
    "AllowedExecutionTenants": {
      "tenant-contoso": true
    },

    // ── Subscription & workspace restrictions ───────────────
    "AllowedAzureSubscriptionIds": [
      "00000000-0000-0000-0000-000000000000"
    ],
    "AllowedLogAnalyticsWorkspaceIds": [
      "11111111-1111-1111-1111-111111111111"
    ],

    // ── Throttling ──────────────────────────────────────────
    "EnableExecutionThrottling": true,
    "ExecutionThrottleWindowSeconds":          60,
    "ExecutionThrottleMaxAttemptsPerWindow":    10,

    // ── Action types — only safe reads enabled ──────────────
    "ActionTypes": [
      { "ActionType": "restart_pod",        "RiskTier": "High",   "Enabled": false },
      { "ActionType": "http_probe",          "RiskTier": "Low",    "Enabled": true  },
      { "ActionType": "dry_run",             "RiskTier": "Low",    "Enabled": true  },
      { "ActionType": "azure_resource_get",  "RiskTier": "Medium", "Enabled": true  },
      { "ActionType": "azure_monitor_query", "RiskTier": "Medium", "Enabled": true  }
    ]
  }
}
