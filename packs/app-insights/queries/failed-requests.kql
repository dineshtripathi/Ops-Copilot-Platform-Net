// Failed HTTP requests in the last 30 minutes
// Workspace schema: AppRequests
AppRequests
| where TimeGenerated > ago(2d) and Success == false
| extend requestName = tostring(Name)
| summarize
    Count = count(),
    FirstSeen = min(TimeGenerated),
    LastSeen = max(TimeGenerated)
    by resultCode = tostring(ResultCode), requestName, url = tostring(Url)
| order by Count desc
| take 25
